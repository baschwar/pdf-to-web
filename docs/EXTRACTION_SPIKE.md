# OpenDataLoader Extraction Spike

Status: implementation foundation in progress. Findings that require a
representative PDF corpus are deliberately marked **unverified**.

## Confirmed from OpenDataLoader 2.4.1 interfaces and schema

- Local deterministic conversion can emit JSON and Markdown in one invocation.
- Hybrid processing is optional and accepts an explicit `off` value. PDF to Web
  always supplies `hybrid="off"` in Phase 1.
- Document content is rooted in `kids` in the published JSON schema.
- Elements may retain `id`, `type`, `page number`, and `bounding box`.
- Headings may expose `heading level`; images may expose an external `source`.
- Lists have list-item children; list items can contain nested content.
- Header and footer elements are schema types, although they are excluded by
  OpenDataLoader unless explicitly requested.
- Reading order is generated by the configured algorithm (`xycut` by default)
  or may come from a tagged PDF structure tree when opted into.

## Current normalized-model decisions

- Every source element becomes a normalized block or is retained as `unknown`.
- Provenance includes the complete raw element, source ID/type/page, geometry,
  and confidence when present.
- OpenDataLoader output remains unchanged under `extraction/raw/`.
- Export-specific settings stay in `project.json`, outside document semantics.
- Inline formatting is represented as optional typed runs, allowing links,
  strong text, and emphasis without storing Gutenberg or HTML in the model.
- Image attachment IDs are optional and are never invented.

## Representative-PDF findings

- The six-document corpus retains page identity and geometry on top-level
  blocks, while some nested list items do not have independent source IDs.
- Source order is preserved; column reading order remains a human review item
  because geometric reordering would risk changing author intent.
- Missing heading levels become provisional H2 blocks with a review issue.
- Adjacent image captions are associated and suppressed as duplicate standalone
  output.
- Table cell text, spans, and geometry are retained by regression fixtures.
- Headers and footers remain explicit unknown blocks with page-furniture roles
  until a reviewer excludes or promotes them.
- Pages with at least five visual assets receive a complex-visual record that
  retains page, asset references, recovered text, and text-recovery ratio.

## Environment finding

The development machine has Python 3.12 and OpenDataLoader PDF 2.4.1. Its
default Java command is Java 8, which is unsupported. Homebrew OpenJDK 17 is
installed at `/usr/local/opt/openjdk@17`; the doctor and extractor discover and
use that runtime without changing the user's global Java configuration.

Restricted sandbox runs caused OpenDataLoader 2.4.1 to abort its JVM or return
incomplete text for several samples. The same operations completed normally
outside the sandbox, including a direct test of external image output. Corpus
evaluation must therefore run in the application's normal local environment;
sandbox results are not valid extraction benchmarks.

Phase 1 preserves OpenDataLoader's externally emitted images under
`extraction/raw/images/`. It also inventories embedded PDF image streams with
pypdf under `extraction/assets/`, including checksums and source pages. The two
sets have distinct provenance: OpenDataLoader images retain structural element
references, while pypdf assets provide a source-stream diagnostic and are only
associated to a page until reviewed.

## Remaining validation boundary

- Confirm WSU hero and section serialization against the exact WSUWP plugin
  versions deployed on the destination site. The local contract shim proves the
  generated shape is stable in WordPress, but it is not production WSU code.
- Media sideloading and destination attachment-ID mapping remain outside Phase
  1B.
- Complex visuals still require authored text equivalents and human approval.

## Initial six-document corpus

The first valid deterministic comparison used six user-supplied PDFs covering
prose, tables, posters, an infographic, and screenshot-heavy instructions. All
12 heuristic/structure-tree runs completed in the normal local environment.

| PDF | Heuristic result | Structure-tree result | Local assets |
| --- | --- | --- | ---: |
| Vaccination admission policy | 97.5% text recovery | 98.9% text recovery | 1 |
| Forrest practicum poster | 100.3% recovery; review-ready baseline | Same output as heuristic | 0 |
| CITI instructions | 96.4% text recovery | 97.9% text recovery | 21 |
| Heart-failure infographic | 65.8% recovery; needs review | 17.1% recovery; conversion blocked | 19 |
| Four-year sample program | 92.6% recovery; review-ready baseline | 93.4% recovery; review-ready baseline | 1 |
| Bloodborne-pathogen guide | 96.4% text recovery | 97.9% text recovery | 0 |

The infographic remains the hardest case in both modes, and heuristic mode is
substantially better there. Mode selection cannot be based on whether a PDF is
tagged. Text recovery, replacement-character rate, page coverage, unknown
elements, and visual review all matter. `review_ready` is a structural
diagnostic, not an accessibility approval.

## First live sample: Soulek.pdf

A two-page tagged Word-generated PDF completed the full local deterministic
pipeline. With structure-tree extraction enabled, OpenDataLoader emitted 13
top-level paragraph elements. Every element retained a source page and bounding
box, but none retained an element ID. The extracted text contained extensive
Unicode replacement characters and headings were not recognized. This is an
important early result: PDF tags cannot be assumed to be the best source merely
because they exist. The default heuristic path produced clean text, but emitted
only one heading element from page 1 of the two-page source. Neither extraction
is usable as-is. Phase 1 should compare default heuristic and structure-tree
results and expose text-integrity and page-coverage diagnostics before
normalization is approved.
